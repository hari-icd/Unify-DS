{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ -n \"$f\" && \"$f\" =~ screens/[^/]+\\.html$ ]] && node \"${CLAUDE_PROJECT_DIR:-.}/scripts/preview.js\" \"$f\"; } || true",
            "statusMessage": "Auto-previewing screen…"
          }
        ]
      }
    ]
  }
}
